<% content_for :title, "Edit wishlist | Mercantile Hub" %>

<section class="content-card">
	<div>
		<p class="eyebrow">Update collection</p>
		<h2>Edit wishlist</h2>
		<p class="muted-copy">Rename this list without changing any of its saved products.</p>
	</div>
	<%= render "form", wishlist: @wishlist %>
</section>
